# PostgreSQL (Docker Compose service "db")
DATABASE_URL="postgresql://viabtp:viabtp@localhost:5432/viabtp?schema=public"

# Server
PORT=4000
NODE_ENV=development

# Auth
JWT_SECRET="change-me-in-production-viabtp-secret"
JWT_EXPIRES_IN="7d"

# CORS (frontend origin)
CLIENT_ORIGIN="http://localhost:5173"

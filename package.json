{
  "name": "easybtp",
  "version": "1.0.0",
  "private": true,
  "description": "EasyBTP - Plateforme de suivi de chantier de construction (web)",
  "scripts": {
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "setup": "npm --prefix backend install && npm --prefix frontend install && npm run db:up && npm --prefix backend run db:setup",
    "backend": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend run dev",
    "seed": "npm --prefix backend run seed"
  }
}
